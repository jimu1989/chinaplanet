"use client";

import Image from "next/image";
import { motion } from "framer-motion";

import type { Language } from "../lib/i18n";
import { translations } from "../lib/i18n";
import { siteConfig } from "../lib/site";

export default function Hero({ language = "ar" }: { language?: Language }) {
  const t = translations[language];
  const isArabic = language === "ar";

  const whatsappMessages: Record<Language, string> = {
    ar: "السلام عليكم، أرغب في التواصل مع كوكب الصين والاستفسار عن خدماتكم.",
    en: "Hello, I would like to contact China Planet and learn more about your services.",
    zh: "您好，我想联系中国星球并了解贵公司的服务。",
  };

  const whatsappUrl = `https://wa.me/${siteConfig.contact.whatsapp}?text=${encodeURIComponent(
    whatsappMessages[language],
  )}`;

  const heroTitle: Record<Language, string> = {
    ar: "اكتشف الصين",
    en: "Discover China",
    zh: "探索中国",
  };

  const heroSubtitle: Record<Language, string> = {
    ar: "بطريقة مختلفة",
    en: "In a Different Way",
    zh: "以一种不同的方式",
  };

  const trust: Record<
    Language,
    {
      location: string;
      audience: string;
      services: string;
    }
  > = {
    ar: {
      location: "السعودية والصين",
      audience: "للأفراد والشركات",
      services: "خدمات متكاملة",
    },
    en: {
      location: "Saudi Arabia & China",
      audience: "For Individuals & Businesses",
      services: "Integrated Services",
    },
    zh: {
      location: "沙特阿拉伯与中国",
      audience: "面向个人与企业",
      services: "一站式综合服务",
    },
  };

  const currentTrust = trust[language];

  return (
    <section
      id="home"
      dir={isArabic ? "rtl" : "ltr"}
      className="relative min-h-[760px] overflow-hidden bg-[#f7f3ec] pt-[76px]"
    >
      <div className="cp-atmosphere cp-atmosphere-red -right-24 top-20 h-[360px] w-[360px] lg:h-[520px] lg:w-[520px]" />
      <div className="cp-atmosphere cp-atmosphere-gold -left-32 bottom-0 h-[300px] w-[300px] lg:h-[450px] lg:w-[450px]" />

      <div className="absolute right-[12%] top-[22%] hidden h-32 w-32 rounded-full border border-[#b5966c]/20 lg:block" />

      <div className="cp-container relative z-10 flex min-h-[684px] items-center">
        <div className="grid w-full items-center gap-14 lg:grid-cols-[1.05fr_0.95fr]">
          <div className={isArabic ? "text-right" : "text-left"}>
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.15 }}
              className="mb-7 flex items-center gap-4"
            >
              <span className="cp-line" />
              <span className="cp-label">
                {language === "ar"
                  ? "TRAVEL · BUSINESS · STUDY"
                  : "TRAVEL · BUSINESS · STUDY"}
              </span>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, y: 24 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.9, delay: 0.3 }}
            >
              <Image
                src="/images/china-planet-logo.png"
                alt="China Planet"
                width={180}
                height={65}
                priority
                className="mb-8 h-auto w-[135px] object-contain"
              />

              <h1 className="max-w-[760px] text-[clamp(3.7rem,8vw,7.7rem)] font-medium leading-[0.94] tracking-[-0.055em] text-[#302923]">
                {heroTitle[language]}
                <span className="mt-2 block text-[#746a62]">
                  {heroSubtitle[language]}
                </span>
              </h1>
            </motion.div>

            <motion.p
              initial={{ opacity: 0, y: 18 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.55 }}
              className="mt-8 max-w-[600px] text-base leading-8 text-[#70665d] sm:text-lg"
            >
              {t.hero.description}
            </motion.p>

            <motion.div
              initial={{ opacity: 0, y: 18 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.7 }}
              className={`mt-9 flex flex-wrap items-center gap-5 ${
                isArabic ? "justify-start" : "justify-start"
              }`}
            >
              <a href="#services" className="cp-button">
                {t.services.explore}
                <span className="text-[#b94b3f]">{isArabic ? "←" : "→"}</span>
              </a>

              <a
                href={whatsappUrl}
                target="_blank"
                rel="noopener noreferrer"
                className="cp-link-line text-xs font-semibold text-[#50473f] transition-colors hover:text-[#b94b3f]"
              >
                {t.contact.label}
              </a>
            </motion.div>

            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ duration: 0.8, delay: 0.95 }}
              className="mt-12 flex flex-wrap items-center gap-x-5 gap-y-3 border-t border-[#ded6ce] pt-6 text-[10px] text-[#8a8078]"
            >
              <span>{currentTrust.location}</span>
              <span className="h-1 w-1 rounded-full bg-[#b94b3f]/70" />
              <span>{currentTrust.audience}</span>
              <span className="h-1 w-1 rounded-full bg-[#b94b3f]/70" />
              <span>{currentTrust.services}</span>
            </motion.div>
          </div>

          <motion.div
            initial={{ opacity: 0, scale: 0.96, y: 20 }}
            animate={{ opacity: 1, scale: 1, y: 0 }}
            transition={{
              duration: 1.15,
              delay: 0.35,
              ease: [0.22, 1, 0.36, 1],
            }}
            className="relative mx-auto w-full max-w-[580px]"
          >
            <div className="absolute -inset-6 rounded-[2rem] bg-[#b94b3f]/[0.045] blur-3xl" />

            <div className="relative overflow-hidden rounded-[2rem] border border-white/70 bg-[#e9e0d5]/50 p-3 shadow-[0_35px_100px_rgba(60,45,35,0.13)]">
              <div className="relative aspect-[0.86] overflow-hidden rounded-[1.5rem]">
                <Image
                  src="/images/hero-china.png"
                  alt={
                    language === "ar"
                      ? "منظر من الصين"
                      : language === "zh"
                        ? "中国风景"
                        : "A view of China"
                  }
                  fill
                  priority
                  sizes="(max-width: 1024px) 90vw, 560px"
                  className="object-cover"
                />

                <div className="absolute inset-0 bg-gradient-to-t from-[#211a16]/55 via-transparent to-white/10" />

                <div className="absolute bottom-6 left-6 right-6 flex items-end justify-between text-white">
                  <div>
                    <p className="text-[9px] font-semibold tracking-[0.28em] opacity-70">
                      CHINA PLANET
                    </p>
                    <p className="mt-2 text-xl font-medium">
                      {language === "ar"
                        ? "الصين أقرب مما تتخيل"
                        : language === "zh"
                          ? "中国，比您想象的更近"
                          : "China, closer than you imagine"}
                    </p>
                  </div>

                  <span className="text-[10px] tracking-[0.25em] opacity-70">
                    01
                  </span>
                </div>
              </div>
            </div>

            <motion.div
              animate={{ y: [0, -8, 0] }}
              transition={{
                duration: 4.5,
                repeat: Infinity,
                ease: "easeInOut",
              }}
              className="absolute -bottom-7 -left-4 hidden rounded-full border border-[#b5966c]/30 bg-[#f7f3ec]/90 px-5 py-3 text-[9px] font-semibold tracking-[0.2em] text-[#746a62] shadow-lg backdrop-blur-md sm:block"
            >
              BEIJING · SHANGHAI · GUANGZHOU
            </motion.div>
          </motion.div>
        </div>
      </div>

      <a
        href="#services"
        className="absolute bottom-7 left-1/2 z-20 flex -translate-x-1/2 flex-col items-center gap-2 text-[8px] font-semibold tracking-[0.35em] text-[#8b8178]"
      >
        SCROLL
        <span className="h-9 w-px bg-gradient-to-b from-[#b94b3f] to-transparent" />
      </a>
    </section>
  );
}
